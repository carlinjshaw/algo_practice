const a = "car"
const b = "rac"

const isPermutation = (a, b) => {
    //make permutations and push them to an array
    //make a permutation,
    //recursive 
    //you have ten positions
    //for each position, swap that for the first 
    if(a == b){return true}

    
}
let perms = [];

const switchLetters = (str, index) => {
    if(index ==0) {perms.push(str)}


}

console.log(isPermutation(a,b))